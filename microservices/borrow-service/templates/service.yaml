apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.borrowService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.borrowService.name }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.borrowService.port }}
      targetPort: {{ .Values.borrowService.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.borrowService.name }}

# Parent chart values.yaml / values-dev.yaml / values-prod.yaml are the single source of truth.
# This stub exists so the subchart can be packaged; all defaults come from the umbrella chart.

global:
  appName: library-e2e
  namespace: chapterone
  imagePullPolicy: IfNotPresent

